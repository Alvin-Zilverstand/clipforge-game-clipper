{
  "name": "clipforge",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "type": "module",
  "description": "Windows-first local game clipping app with replay buffer and event auto-clipping.",
  "scripts": {
    "dev": "vite ui --host 127.0.0.1",
    "prepare:ffmpeg": "powershell -ExecutionPolicy Bypass -File ./scripts/prepare-ffmpeg-sidecar.ps1",
    "tauri": "pnpm exec tauri",
    "build": "vite build ui",
    "desktop:dev": "pnpm run prepare:ffmpeg && pnpm run tauri dev",
    "desktop:build": "pnpm run prepare:ffmpeg && pnpm run tauri build",
    "preview": "vite preview ui --host 127.0.0.1"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "vite": "^7.0.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}

